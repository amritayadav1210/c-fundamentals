#include<stdio.h>
//to swap the values
int main(){
int c, d, i ,x;
do{
printf("\nEnter c:");
   // scanf("%d",&c);
    printf("\nEnter d  :");
    //scanf("%d",&d);

    x=d;
  printf("\nx=%d",x);
  d=c;
  printf("\n d=%d",d);
  c=x;
  printf("\n c=%d",c);
}while(i<10);
i=0,i++;

    return 0;

}